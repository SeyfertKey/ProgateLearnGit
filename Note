git init
    untuk memulai "init adalah singkatan dari initialize"

git add file_name
    Untuk memilih sebuah file

git commit -m "your message"
    Untuk merekam file yang dipilih

git remote add remote_name URL
    untuk mendaftarkan remote, kamu akan memerlukan nama. Biasanya, pengembang menggunakan nama origin
        contoh git remote add origin https://prog-8.com/mysite.git
        origin = nama remote
        remote_name URL = URL remote

$ git remote set-url origin remote_name URL
    untuk merubah origin URL semisal dari https://prog-8.com/mysite.git ke URL baru seperti https://github.com/SeyfertKey/mysite.git

git push origin master 
    mengunggah ke remote

git pull origin master
    Mengunduh file seperti ini disebut pull. 

git status
    tampilkan file yang telah berubah.

git diff
    tampilkan konten yang sudah dimodifikasi

# Tips
    CRTL + Shift + V : Paste ke Terminal (CMD)
    CRTL + Shift + C : Copy ke Terminal (CMD)